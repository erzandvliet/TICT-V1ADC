#include <stdio.h>

int main()
{
	int a, i, d; 
	printf("Getal tussen 0 en 100: ");
	scanf("%d", &a);
	for (i = 1; i < 13; i++){
		d = a * i;
		printf("%6d maal %6d is %6d \n", i, a, d);
	}
	return 0;
}
