#include <stdio.h>
#include <string.h>

int main(int argc, char **argv)
{
	char string[81] = {};
	printf("Geef string: ");
	fgets(string,80,stdin);
	
	for (int i = strlen(string)-1; i >= 0; --i) {
		printf("%c", string[i]);
	}

	return 0;
}
